import { NewWindowDirective } from './new-window.directive';

describe('NewWindowDirective', () => {
  it('should create an instance', () => {
    const directive = new NewWindowDirective();
    expect(directive).toBeTruthy();
  });
});
